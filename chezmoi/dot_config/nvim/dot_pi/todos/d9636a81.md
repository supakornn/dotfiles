{
  "id": "d9636a81",
  "title": "Migrate Neovim treesitter config to nvim-treesitter main with backup",
  "tags": [
    "nvim",
    "treesitter",
    "migration"
  ],
  "status": "done",
  "created_at": "2026-03-30T12:46:37.268Z"
}

Backed up the existing treesitter-related config to `backups/treesitter-migration-2026-03-30/`, migrated `lua/plugins/treesitter.lua` to the `nvim-treesitter` main-branch API, migrated `nvim-treesitter-textobjects` to direct setup + explicit keymaps, added explicit `nvim-ts-autotag` setup, updated `lazy-lock.json` to main-branch commits, added `tree-sitter-cli` to `packages/bundle`, installed it locally, installed/updated the configured parsers, and verified the config loads with `nvim --headless '+qa'`.
